# Blog content

Place one English and/or Russian `.mdx` file per article here. Keep the slug identical between translations and add frontmatter fields: `title`, `description`, `date`, `readingTime`, `tags`, and `cover`.

The demo UI currently reads article metadata from `src/content/blog.ts`; replace those entries when adding real MDX content.
